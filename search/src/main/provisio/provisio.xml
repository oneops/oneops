<runtime>
  <archive
    prefix="oneops/dist/"
    name="${basedir}/target/${project.artifactId}-${project.version}.tar.gz"/>
  <fileSet to="/">
     <directory path="${basedir}/target">
       <include>search*.jar</include>
     </directory>
   </fileSet>
</runtime>
