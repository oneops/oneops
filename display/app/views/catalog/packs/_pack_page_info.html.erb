<% desc = (@pack || @platform).ciAttributes.description
   show_versions = @versions && @versions.size > 1 %>
<%= page_info do %>
  <%= "#{desc}" if desc.present? && desc.size > @pack.ciName.size %>
  <% if show_versions %>
    <br><b>All versions: </b>
    <%= render 'catalog/packs/pack_version_list', :versions => @versions %>
  <% end %>
<% end %>
