$j("#cost_report_results").html("<%= escape_javascript(render('cost')) %>");
replace_state("<%= url_for(:ns_path    => @ns_path,
                           :start_date => @start_date,
                           :end_date   => @end_date,
                           :interval   => @interval) %>");
hide_modal();
