$j("#<%= request.original_url.downcase.include?('timeline/') ? 'timeline' : 'release' %>_details").html("<%= escape_javascript(render('release_details', :release => @release)) %>");
window.scroll(0, 0);
