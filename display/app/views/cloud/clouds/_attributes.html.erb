<%= render 'base/shared/edit_form',
           :resource    => @cloud,
           :content     => 'cms/cis/ci',
           :remote      => true,
           :read_only   => !manages_access?,
           :form_url    => cloud_path(@cloud),
           :destroy_url => cloud_path(@cloud),
           :cancel_url  => clouds_path %>
