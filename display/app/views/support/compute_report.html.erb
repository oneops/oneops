<% support_page_header('compute', 'Compute Report') %>
<%= section_panel 'Compute Report', :width => 'triple', :position => 'left' do %>
  <%= render('base/shared/graph_sunburst',
             :data_url   => compute_support_path,
             :email      => true,
             :table_view => true) %>
<% end %>
