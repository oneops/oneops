<runtime>
  <archive
    prefix="oneops/dist/"
    name="${basedir}/target/${project.artifactId}-${project.version}.tar.gz"/>
  <fileSet to="/">
     <directory path="${basedir}/target">
       <include>oneops-admin-${project.version}-*.tar.gz</include>
     </directory>
   </fileSet>
</runtime>
