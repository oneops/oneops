<div class="alert alert-info">
  <%= icon('refresh fa-spin') %>Design pull is already in progress, started <strong><%= time_ago_in_words(@environment.updated_timestamp) %></strong>.
</div>
<script>
  setTimeout(function () {
               $j.get("<%= pull_status_assembly_transition_environment_path(@assembly, @environment) %>");
             },
             15000);
</script>
