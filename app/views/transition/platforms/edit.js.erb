$j("#platform_configuration .section-content").html('<%= escape_javascript(render('base/shared/edit_form',
                                                                                  {:resource    => @platform,
                                                                                   :content     => 'platform_details_content',
                                                                                   :form_url    => {:action => :update, :id => @platform.id}})) %>');
<% unless request.method == "GET" %>
  $j("#page_alert").html("<%= escape_javascript(page_alert) %>");
<% end %>
