<%= render('base/shared/graph_bar', :data => @histogram, :height => 150) if @histogram %>
<% if @notifications %>
  <%= notification_list @notifications, :toolbar => nil %>
<% else %>
  <p class="text-error"><%= icon('warning-sign') %> <strong>Failed to retrieve notifications</strong></p>
<% end %>
