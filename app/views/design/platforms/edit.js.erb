$j("#platform_configuration .section-content")
    .html("<%= escape_javascript(render('base/shared/edit_form',
                                        {:resource    => @platform,
                                         :content     => 'platform_details_content',
                                         :form_url    => {:action => :update, :id => @platform.id}})) %>");
$("page_alert").update("<%= escape_javascript(page_alert) %>");
